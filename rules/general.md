# General Rules

- Keep durable requirements, decisions, and progress in files, not chat.
- Prefer small modules with explicit responsibilities.
- Separate hardware access, domain rules, UI, and persistence.
- Use descriptive AGV/fertigation names such as `route_planner` and `flow_rate_sensor`.
- Store configuration in environment variables or sanitized example files.
